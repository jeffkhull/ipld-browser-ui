export const regexMatchers = {
  wordThatStartsWithAtSymbol: /^@(\w+)$/,
  notASpaceOrLineEnding: /^(\s|$)/,
  isPunctuationMark: /[.!?\\-]/,
  lastSentence: /([^.!?]*$)/,
  matchAnySpace: / /,
  matchAtSymbol: /@/,
}
